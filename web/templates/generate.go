// Package templates holds tend-ui's templ components. The *_templ.go files are
// generated from the .templ sources and committed, so `go build` works without
// the templ CLI; regenerate with `go generate ./...` (needs templ on PATH).
package templates

//go:generate templ generate
